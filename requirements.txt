# Public demo build — runs entirely on synthetic data (demo mode).
# Only the lightweight web + scheduling stack is needed here.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
jinja2>=3.1.4
pydantic>=2.9.0
pydantic-settings>=2.6.0
cachetools>=5.5.0
httpx>=0.27.0
apscheduler>=3.10.4
python-dotenv>=1.0.1

# ── Live-only dependencies (NOT required for the demo) ──────────────
# The production tool also uses these to talk to real systems. They are
# imported lazily / behind guards, so the demo runs without them. Install
# from requirements-live.txt if you wire up real connectors:
#   simple-salesforce, playwright, chromadb, sentence-transformers,
#   msal, nats-py
